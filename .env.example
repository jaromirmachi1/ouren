# Marketing site (Vercel project: ouren.vercel.app)
# Must be set BEFORE build — triggers /admin rewrite to the portal deployment.
# Do NOT use ouren.vercel.app/admin — point to the separate dashboard Vercel project.
# Example: https://ouren-portal.vercel.app/admin
VITE_PORTAL_URL=
